/**
 * Loading UI for Dashboard Routes
 * Shows loading state while dashboard pages are loading
 */

import { PageLoading } from '@/app/components/ui/loading';

export default function Loading() {
  return <PageLoading />;
}

